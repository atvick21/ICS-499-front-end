import { PeriodicTableModel } from './periodic-table-model';

describe('PeriodicTableModel', () => {
  it('should create an instance', () => {
    expect(new PeriodicTableModel()).toBeTruthy();
  });
});
